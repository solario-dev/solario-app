services:
  solario:
    image: solario
    ports:
      - "5000:5000"
    environment:
      MONGODB_CONNECTION_STRING: mongodb://mongo:27017
      MONGODB_DATABASE: SolarioDb

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"

# docker compose up --build